<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis">
	
	<!-- 查询调用链异常堆栈信息 -->
	<select id="query" parameterType="java.util.HashMap" resultType="java.lang.String">
		SELECT money
		FROM t_sharding 
		WHERE id=#{id}
	</select>
	
	<insert id="insert">
		INSERT INTO t_sharding(id, money, status, create_time) values (#{id}, #{money}, #{status}, now())
	</insert>

</mapper>